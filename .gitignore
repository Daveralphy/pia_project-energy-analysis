# Python cache and compiled files
__pycache__/
*.pyc

# Virtual environment folders
.venv/
venv/
env/

# Secrets - VERY IMPORTANT
# This line prevents your .env file with API keys from ever being committed.
.env

# Data files generated by the pipeline
# We don't want to commit large data files to the repo.
# The app should generate them on the server when the pipeline runs.
data/raw/
data/processed/
data/output/

